# Secrets / local env — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials*

# Go build output
/backend/qopt
/backend/qopt-server
# launcher/Docker build the binaries at the repo root
/qopt
/qopt-server
/qopt-seed
*.out

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/*.tsbuildinfo

# Local demo databases
*.db
*.sqlite

# OS / editor
.DS_Store
